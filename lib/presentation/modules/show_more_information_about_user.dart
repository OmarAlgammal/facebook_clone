import 'package:flutter/material.dart';

class ShowMoreInformationAboutUser extends StatelessWidget {
  const ShowMoreInformationAboutUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.start,
        children: [
          
        ],
      ),
    );
  }
}
